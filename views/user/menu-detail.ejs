<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Food App UI</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/menu.css">

    <style>

.product-image {
    width: 100%;
    height: 50vh;
    object-fit: cover;
}

.product-info {
    padding: 20px;
    flex-grow: 1;
    display: flex;
    flex-direction: column;
}

.product-name {
    margin: 0;
    font-size: 28px;
}

.product-price {
    font-weight: bold;
    margin: 10px 0;
    font-size: 24px;
}
        .close-btn {
    position: relative;
    top: 20px;
    left: 20px;
    background-color: rgba(0, 0, 0, 0.5);
    color: white;
    border: none;
    border-radius: 50%;
    width: 40px;
    height: 40px;
    font-size: 24px;
    display: flex;
    justify-content: center;
    align-items: center;
    cursor: pointer;
    z-index: 10;
}

        body {
    background-color: #f5f5f5;
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
}

.product-image {
    width: 100%;
    height: 50vh;
    object-fit: cover;
}

.app-container {
    max-width: 400px;
    width: 100%;
    height: 100vh;
    margin: 0 auto;
    background-color: white;
    border-radius: 20px;
    overflow-y: auto;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    display: flex;
    flex-direction: column;
}

/* Add this to create space for the bottom nav */
.content-wrapper {
    flex: 1;
    overflow-y: auto;
    padding-bottom: 70px;
}

.bottom-nav {
    background-color: #FFF;
    display: flex;
    justify-content: space-around;
    padding: 15px;
    position: sticky;
    bottom: 0;
    width: 100%;
    max-width: 400px;
    border-top: 1.25px solid rgba(229,231,235,var(--tw-border-opacity));
}
.header {
    background-color: FFF;
    color: white;
    padding: 15px;
    border-top-left-radius: 20px;
    border-top-right-radius: 20px;
}
.search-bar {
    background-color: #f5f5f5;
    border-radius: 20px;
    padding: 10px 15px;
    margin: 10px 15px;
    display: flex;
    align-items: center;
}
.category-filters {
    display: flex;
    gap: 10px;
    padding: 10px 15px;
}
.filter-btn {
    padding: 5px 15px;
    border-radius: 15px;
    font-size: 14px;
    cursor: pointer;
    transition: all 0.3s;
}
.active-filter {
    background-color: #B22222;
    color: white;
}
.food-item {
    display: flex;
    align-items: center;
    padding: 15px;
    border-bottom: 1px solid #eee;
}
.food-image {
    width: 60px;
    height: 60px;
    background-color: #eee;
    border-radius: 10px;
    margin-right: 15px;
}

.food-image img{
    width: 100%;
    height: 100%;
    aspect-ratio: auto;
}
.bottom-nav {
    background-color: #fff;
    display: flex;
    justify-content: space-around;
    padding: 15px;
    margin-top: auto;
    width: 100%;
    max-width: 400px;
}

.nav-item {
    color: white;
    display: flex;
    flex-direction: column;
    align-items: center;
}
.section {
    display: none;
}
.section.active {
    display: block;
}

.quantity-control {
    display: flex;
    align-items: center;
    margin-top: auto;
}

.quantity-btn {
    width: 40px;
    height: 40px;
    background-color: #f0f0f0;
    border: none;
    border-radius: 50%;
    font-size: 24px;
    cursor: pointer;
}

.quantity {
    margin: 0 20px;
    font-size: 20px;
}

.add-to-cart {
    flex-grow: 1;
    background-color: #B22222;
    color: white;
    border: none;
    padding: 15px;
    border-radius: 5px;
    margin-left: 20px;
    cursor: pointer;
    font-size: 18px;
}
    </style>
       
</head>
<body class="pb-16">
    <div class="app-container">
        <a href="/user/menu">
            <button class="close-btn">×</button>
        </a>
        <img src="<%= food.food_image_url %>" alt="Muffin burger" class="product-image">
        <div class="product-info">
            <h2 class="product-name"><%= food.food_name %></h2>
            <p class="product-price">฿<%= food.food_price %></p>
            <p class="additional-info">ข้อความเพิ่มเติม (ถ้ามี)</p>
            <div class="quantity-control">
                <button class="quantity-btn minus" onclick="updateQuantity(-1)">−</button>
                <span id="quantity" class="quantity">1</span>
                <% let quantity = 1; %>
                <button class="quantity-btn plus" onclick="updateQuantity(1)">+</button>
                <a href="/cart/add-to-cart/<%= food.FoodID %>/<%= quantity %>">
                    <button class="add-to-cart" type="submit" onsubmit="addToCart(foodID, qty)">Add to Cart ฿<span id="total-price"><%= food.food_price %></span></button>
                </a>
            </div>
        </div>
    </div>

 
    <script>
        quantity = 1;
        const basePrice = <%= food.food_price %>;

        function updateQuantity(change) {
            quantity = Math.max(1, quantity + change);
            document.getElementById('quantity').textContent = quantity;
            updateTotalPrice();
        }

        function updateTotalPrice() {
            const totalPrice = basePrice * quantity;
            document.getElementById('total-price').textContent = totalPrice;
        }

        function addToCart(id, qty) {
            window.location.href = `http://localhost:3200/add-to-cart/:${id}/:${qty}`;
            alert(`Added ${quantity} <%= food.food_name %>(s) to cart. Total: ฿${basePrice * quantity}`);
        }
    </script>
</body>
</html>